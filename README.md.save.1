# 路由系統

##  專案架構
- Hroute是一個基於 Linux 環境開發的輕量級路由系統框架。
- 本專案採用 C 語言編寫，旨在提供一個高效率、易於擴充的網路服務解決方案。透過模組化的架構設計，將不同的網路協議（如 DHCP, DNS, NAT）獨立開發與編譯，並共享底層通用工具庫，實現了高度的程式碼復用性與維護性。
```
route/                  # 專案根目錄
├── Makefile            # 頂層建置腳本，負責遞迴調度各模組的編譯流程與清理作業
├── bin/                # 編譯產物目錄，存放最終生成的二進位執行檔（如 my_dhcp、my_dns）
│
├── common/             # 共用模組（通用函式庫）
│   ├── Makefile        # 編譯共用模組原始碼為目的檔（.o）的自動化建置檔
│   ├── include/        # 共用標頭檔目錄（.h），定義共用資料結構、常數與 API 介面
│   └── src/            # 共用模組原始碼（.c），實作如日誌系統（Logger）、校驗和（Checksum）等通用底層功能
│
├── dhcp/               # DHCP 服務模組
    ├── Makefile        # DHCP 模組建置腳本，編譯模組原始檔並鏈結 common 目的檔生成可執行檔
    ├── include/        # DHCP 模組私有標頭檔（.h），定義 DHCP 協定封包格式與狀態機介面
    └── src/            # DHCP 協定邏輯原始碼（.c），實作 DISCOVER 解析、OFFER/REQUEST/ACK 等核心流程
```
## 架構設計介紹

- 模組化編譯架構：每個子目錄擁有獨立的 Makefile 與 include 環境，方便開發者獨立測試單一模組。

- 統一的二進位管理：編譯結果統一存放於 bin/ 目錄，簡化部署與安裝流程。

- 嚴謹的目錄規範：區分 src (實作) 與 include (規格定義)，確保 API 介面清晰，降低後續擴充的難度。

- DHCP 模組：動態主機配置協議實作，負責自動化 IP 位址分配與管理。

- DNS 模組：支援網域名稱解析服務。

- NAT 模組：實作網路位址轉換，提供內外網封包轉發功能。


## 部屬說明

1. **取得最新發布檔**
   至 GitHub Releases 下載最新版本的壓縮檔（例如 `Hroute-v1.0.0-linux-x86_64.tar.gz`），並傳輸至目標主機。

2. **解壓縮並進入目錄內部**
```
tar -zxvf Hroute-v1.0.0-linux-x86_64.tar.gz
cd Hroute-v1.0.0-linux-x86_64/
```



- 1. 進入到放執行檔的目錄中
```
cd bin/
```
- 2. 使用最高權限執行被編譯好的執行檔
```
sudo ./my_dhcp
```

- **執行專案**
- 1. 進入到放置配檔的目錄中
```
cd etc/
```
- 2. 使用最高權限編輯配置檔案
```
sudo nano dhcp.conf
```
- 3. 將以下內容改成你需要的配置內容
```
# --- Network Interface Settings ---
# The network interface the DHCP server should listen on (Example: eth0, enp0s3, wlan0)
interface=enp5s0

# --- IP Address Settings ---
# The Static IP Addesss Of This DHCP Server.
server_ip=192.168.1.1
# Network Submask For Client
netmask=255.255.255.0
# Default Gateway (Usually is Server IP)
router=192.168.1.1

# --- IP Pool Settings ---
# Start For Allocated IP (Example: Yu can start IP allcation from .200 )
ip_start=192.168.1.200
# End Of Allocated IP (Accourding To POOL_SIZE Determines The Range Of IP Allcation )
ip_end=192.168.1.250

# --- Release Settings ---
# Release Time (Unit：second)，3600 stands for an hour.
lease_time=3600
```
